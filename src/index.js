import React from 'react';
import ReactDOM from 'react-dom/client';
import ConvereterCurrency from './Converter';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <ConvereterCurrency />
  </React.StrictMode>
);